---
description: Capture a correction as a durable, replayable learning in PromptWise.
argument-hint: [what went wrong -> the fix]
---

Use the PromptWise `capture_learning` tool to store the correction described below.
Infer a sensible `category` (e.g. style, security, api-misuse), split it into the
`mistake` and the `correction`, and attach the current project name. Confirm what was
stored.

$ARGUMENTS
