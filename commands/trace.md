---
description: Export and verify the hash-chained AI-change audit trace for this session.
---

Use the PromptWise `export_audit` tool to emit the portable audit trail, then report:
the record count, whether the hash chain verifies, and a one-line summary of the
changes recorded. If `search_trace` is available and the user named a topic in
`$ARGUMENTS`, search the trace for it as well.
