---
description: Split a large document into retrievable shards via PromptWise.
argument-hint: [document text or path]
---

Use the PromptWise `shard_doc` tool to split the document below into coherent, retrievable shards and report the shard map (titles + boundaries).

$ARGUMENTS
