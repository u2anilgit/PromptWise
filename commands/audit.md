---
description: Export and verify the hash-chained audit trace via PromptWise.
argument-hint: [optional focus]
---

Use the PromptWise `export_audit` tool to emit the portable audit trace, then confirm the hash chain verifies and summarize what changed (records, files touched, chain status).

$ARGUMENTS
