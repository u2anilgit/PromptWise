"""PromptWise V3 — Unified token-aware prompt routing and cost optimization."""

import sys

if sys.version_info < (3, 10):
    raise RuntimeError(f"PromptWise V3 requires Python 3.10+ (running {sys.version_info.major}.{sys.version_info.minor})")

__version__ = "3.0.0"
