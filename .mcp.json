{
  "mcpServers": {
    "promptwise": {
      "command": "python",
      "args": [
        "-m",
        "promptwise.server"
      ],
      "cwd": "${projectDir}"
    },
    "promptwise-v2": {
      "command": "python",
      "args": [
        "-m",
        "promptwise_v2.integrations.mcp_server_v2"
      ],
      "cwd": "${projectDir}"
    }
  }
}
