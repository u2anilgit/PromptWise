[
  {
    "description": "Route request to appropriate model tier based on intent, stakes, and budget",
    "inputSchema": {
      "properties": {
        "days_elapsed_in_month": {
          "type": "integer"
        },
        "intent": {
          "default": "auto",
          "enum": [
            "auto",
            "extract",
            "classify",
            "summarize",
            "question",
            "code",
            "analysis",
            "agent_loop",
            "research"
          ],
          "type": "string"
        },
        "monthly_budget_usd": {
          "type": "number"
        },
        "provider": {
          "default": "claude",
          "type": "string"
        },
        "provider_spend_usd": {
          "description": "Spend already incurred for this provider (e.g. today) -- enables a hard budget-cap reroute before the call, if the provider has a configured daily_cap_usd",
          "type": "number"
        },
        "stakes": {
          "default": "auto",
          "enum": [
            "auto",
            "low",
            "medium",
            "high"
          ],
          "type": "string"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "route_request"
  },
  {
    "description": "Rewrite prompt with role framing and filler removal",
    "inputSchema": {
      "properties": {
        "model": {
          "default": "claude-sonnet-4-6",
          "type": "string"
        },
        "role": {
          "default": "general",
          "enum": [
            "general",
            "developer",
            "analyst",
            "manager",
            "security",
            "IT",
            "designer",
            "writer",
            "researcher",
            "pm"
          ],
          "type": "string"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "rewrite_prompt"
  },
  {
    "description": "Compress context to fit token budget by dropping low-value content",
    "inputSchema": {
      "properties": {
        "context": {
          "type": "string"
        },
        "model": {
          "default": "claude-sonnet-4-6",
          "type": "string"
        },
        "token_budget": {
          "default": 2000,
          "minimum": 100,
          "type": "integer"
        }
      },
      "required": [
        "context"
      ],
      "type": "object"
    },
    "name": "optimize_context"
  },
  {
    "description": "Apply caveman compression: remove articles, filler, pleasantries, hedging",
    "inputSchema": {
      "properties": {
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "compress_prompt"
  },
  {
    "description": "Plan cache breakpoints for prompt reuse",
    "inputSchema": {
      "properties": {
        "expected_reuse_count": {
          "default": 2,
          "minimum": 1,
          "type": "integer"
        },
        "messages": {
          "items": {
            "properties": {
              "content": {
                "type": "string"
              },
              "label": {
                "type": "string"
              },
              "role": {
                "enum": [
                  "system",
                  "user",
                  "assistant"
                ],
                "type": "string"
              }
            },
            "required": [
              "role",
              "content"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "model": {
          "default": "claude-sonnet-4-6",
          "type": "string"
        }
      },
      "required": [
        "messages"
      ],
      "type": "object"
    },
    "name": "plan_cache"
  },
  {
    "description": "Exact-match lookup in the local result cache (ExactCache): given identical (tool, request) input to a prior cache_store call, return the stored result instead of recomputing. Hash-based only, no embeddings/similarity threshold. Offline.",
    "inputSchema": {
      "properties": {
        "request": {
          "description": "the request payload to hash (string or object)"
        },
        "tool": {
          "description": "name of the tool/skill this result belongs to",
          "type": "string"
        }
      },
      "required": [
        "tool",
        "request"
      ],
      "type": "object"
    },
    "name": "cache_lookup"
  },
  {
    "description": "Store a result in the local exact-match cache (ExactCache) for later cache_lookup hits. Refuses to store (reports why) if category is medical/legal/financial/personalized/health, or if the request/result contains PII or secrets per security.scanner. Offline.",
    "inputSchema": {
      "properties": {
        "category": {
          "default": "",
          "description": "e.g. 'medical'/'legal'/'financial'/'personalized'/'health' are never cached",
          "type": "string"
        },
        "request": {
          "description": "the request payload to hash (string or object)"
        },
        "result": {
          "description": "the result to cache (any JSON-serializable value)"
        },
        "tool": {
          "description": "name of the tool/skill this result belongs to",
          "type": "string"
        },
        "ttl_seconds": {
          "description": "override the default 1h TTL; 0 means no expiry",
          "type": "integer"
        }
      },
      "required": [
        "tool",
        "request",
        "result"
      ],
      "type": "object"
    },
    "name": "cache_store"
  },
  {
    "description": "Hit/miss/entry-count/hit-rate report for the local exact-match cache (ExactCache), broken down by category. Purges expired entries first by default.",
    "inputSchema": {
      "properties": {
        "purge_expired": {
          "default": true,
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "name": "cache_stats"
  },
  {
    "description": "Batch multiple tasks into one prompt to reduce overhead",
    "inputSchema": {
      "properties": {
        "model": {
          "default": "claude-sonnet-4-6",
          "type": "string"
        },
        "role": {
          "default": "general",
          "type": "string"
        },
        "tasks": {
          "items": {
            "type": "string"
          },
          "maxItems": 5,
          "minItems": 2,
          "type": "array"
        }
      },
      "required": [
        "tasks"
      ],
      "type": "object"
    },
    "name": "batch_prompts"
  },
  {
    "description": "Compress conversation for fresh thread handoff",
    "inputSchema": {
      "properties": {
        "conversation": {
          "type": "string"
        },
        "max_tokens": {
          "default": 500,
          "maximum": 2000,
          "minimum": 100,
          "type": "integer"
        },
        "model": {
          "default": "claude-sonnet-4-6",
          "type": "string"
        }
      },
      "required": [
        "conversation"
      ],
      "type": "object"
    },
    "name": "summarize_thread"
  },
  {
    "description": "Advisory cost comparison for the same estimated token count: the claude entry uses this project's live registry pricing (the real source of truth); non-claude entries (OpenAI/Gemini/etc, when include_external=true, the default) come from a static, offline, user-editable reference catalog (config/external_models.yaml) -- never live-fetched, never consulted by routing, purely informational. Each result is tagged advisory:true/false so callers never mistake a reference price for a live one.",
    "inputSchema": {
      "properties": {
        "include_external": {
          "default": true,
          "type": "boolean"
        },
        "model": {
          "default": "claude-sonnet-4-6",
          "type": "string"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "compare_providers"
  },
  {
    "description": "Run security check (secrets, injection, PII, destructive, permissions). Supply-chain OSV.dev lookups are off by default (air-gap safe); set allow_network=true to opt in.",
    "inputSchema": {
      "properties": {
        "allow_network": {
          "default": false,
          "type": "boolean"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "security_check"
  },
  {
    "description": "Scan user input for prompt injection or jailbreak attempts",
    "inputSchema": {
      "properties": {
        "text": {
          "type": "string"
        },
        "threshold": {
          "default": 0.7,
          "type": "number"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "prompt_injection"
  },
  {
    "description": "Scan code for OWASP Top-10 vulnerabilities",
    "inputSchema": {
      "properties": {
        "code": {
          "type": "string"
        },
        "language": {
          "default": "python",
          "type": "string"
        }
      },
      "required": [
        "code"
      ],
      "type": "object"
    },
    "name": "owasp_scan"
  },
  {
    "description": "Scan a model response for PII leaks, injection echoes, canary leaks, and responsible-AI signals (factual grounding vs. provided sources, bias/fairness, ethical disclosure). Pass a canary token (issued via the indirect-injection canary) to flag if content that flowed through tool output/RAG leaks back into the response. Advisory.",
    "inputSchema": {
      "properties": {
        "canary": {
          "default": "",
          "description": "Canary token placed in tool-output/RAG content; flags a leak if it reappears here",
          "type": "string"
        },
        "original_prompt": {
          "default": "",
          "type": "string"
        },
        "response": {
          "type": "string"
        },
        "sources": {
          "default": "",
          "description": "Source/context text the response should be grounded in; enables grounding checks",
          "type": "string"
        }
      },
      "required": [
        "response"
      ],
      "type": "object"
    },
    "name": "scan_response"
  },
  {
    "description": "Benchmark the prompt-injection detector against a bundled offline attack+benign corpus and report measured precision/recall/F1/accuracy plus the actual false positives/negatives (a real number, not a claim). Offline by default (air-gap safe); an optional live PINT-style corpus fetch is gated behind allow_network=true.",
    "inputSchema": {
      "properties": {
        "allow_network": {
          "default": false,
          "type": "boolean"
        },
        "corpus_path": {
          "default": "",
          "type": "string"
        },
        "pint_url": {
          "default": "",
          "type": "string"
        },
        "threshold": {
          "default": 0.0,
          "type": "number"
        }
      },
      "type": "object"
    },
    "name": "benchmark_injection"
  },
  {
    "description": "Self-service accept/sign-off for a known residual risk: marks a specific finding (identified by check+detail) as accepted, with an optional expiry date. One call, no approval workflow. Does not change run_security_suite's pass/fail or risk_score -- purely a governance annotation.",
    "inputSchema": {
      "properties": {
        "accepted_by": {
          "default": "",
          "type": "string"
        },
        "check": {
          "type": "string"
        },
        "detail": {
          "type": "string"
        },
        "expires_at": {
          "default": "",
          "description": "ISO-8601 UTC timestamp (e.g. '2026-12-31T00:00:00Z') after which this acceptance lazily expires; compared lexicographically, so the format must match exactly or expiry will be computed incorrectly. Omit for no expiry.",
          "type": "string"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "check",
        "detail",
        "reason"
      ],
      "type": "object"
    },
    "name": "accept_risk"
  },
  {
    "description": "List residual-risk register entries, optionally filtered by computed status (open/accepted/expired -- expiry is computed at read time, never mutates stored rows).",
    "inputSchema": {
      "properties": {
        "status": {
          "default": "",
          "enum": [
            "open",
            "accepted",
            "expired"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_risk_register"
  },
  {
    "description": "Dry-run score candidate injection-corpus cases (from a manually curated JSON file) against the current live detector, without mutating any corpus or history. Use before promote_corpus_candidates to see which candidate ids the detector already gets right vs. wrong.",
    "inputSchema": {
      "properties": {
        "path": {
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "name": "review_corpus_candidates"
  },
  {
    "description": "Merge human-reviewed candidate cases into the live injection corpus (corpus/injection_corpus.json). Only ids in approve_ids (or, if approve_ids is omitted, every candidate not listed in reject_ids) are merged. Records an append-only corpus_history row with before/after precision/recall/F1. Requires a non-empty reviewer name -- this is a human-review gate, not an automated classifier.",
    "inputSchema": {
      "properties": {
        "approve_ids": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "path": {
          "type": "string"
        },
        "reject_ids": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "reviewer": {
          "type": "string"
        }
      },
      "required": [
        "path",
        "reviewer"
      ],
      "type": "object"
    },
    "name": "promote_corpus_candidates"
  },
  {
    "description": "Classify a task (greenfield/brownfield/regulated) and return an ordered workflow of PromptWise's own skill packs + tools to run (PRD -> design -> stories -> TDD -> review). No third-party frameworks.",
    "inputSchema": {
      "properties": {
        "brownfield": {
          "description": "Override auto-detection of brownfield (existing-code) change",
          "type": "boolean"
        },
        "regulated": {
          "description": "Override auto-detection of regulated/compliance context",
          "type": "boolean"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "plan_workflow"
  },
  {
    "description": "Create a development task with an effort estimate; tracks effort, tokens, and cost",
    "inputSchema": {
      "properties": {
        "estimate_hours": {
          "default": 0,
          "type": "number"
        },
        "status": {
          "default": "todo",
          "enum": [
            "todo",
            "in_progress",
            "blocked",
            "done"
          ],
          "type": "string"
        },
        "tags": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "title"
      ],
      "type": "object"
    },
    "name": "add_task"
  },
  {
    "description": "Update a task's status, actual hours, tokens, or cost (set or increment)",
    "inputSchema": {
      "properties": {
        "actual_hours": {
          "type": "number"
        },
        "add_cost": {
          "type": "number"
        },
        "add_tokens": {
          "type": "number"
        },
        "cost_usd": {
          "type": "number"
        },
        "status": {
          "enum": [
            "todo",
            "in_progress",
            "blocked",
            "done"
          ],
          "type": "string"
        },
        "task_id": {
          "type": "string"
        },
        "tokens": {
          "type": "number"
        }
      },
      "required": [
        "task_id"
      ],
      "type": "object"
    },
    "name": "update_task"
  },
  {
    "description": "List tracked tasks, optionally filtered by status",
    "inputSchema": {
      "properties": {
        "status": {
          "enum": [
            "todo",
            "in_progress",
            "blocked",
            "done"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_tasks"
  },
  {
    "description": "Effort (estimate vs actual), token, and cost rollup across all tasks",
    "inputSchema": {
      "properties": {},
      "type": "object"
    },
    "name": "task_report"
  },
  {
    "description": "Lint Mermaid diagram source (type, bracket/quote balance) so it renders",
    "inputSchema": {
      "properties": {
        "source": {
          "type": "string"
        }
      },
      "required": [
        "source"
      ],
      "type": "object"
    },
    "name": "validate_mermaid"
  },
  {
    "description": "Detect organizational role from prompt context",
    "inputSchema": {
      "properties": {
        "file_type": {
          "type": "string"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "detect_role"
  },
  {
    "description": "Parse a multi-step prompt into a DAG and execute with a failure strategy. Pass 'tasks' (with depends_on / file) to instead emit a safe parallel wave plan (which tasks can run at once) for the caller to dispatch.",
    "inputSchema": {
      "properties": {
        "fan_out_cap": {
          "default": 8,
          "description": "Max tasks per parallel wave",
          "type": "integer"
        },
        "strategy": {
          "default": "fallback",
          "enum": [
            "stop",
            "retry",
            "fallback",
            "all"
          ],
          "type": "string"
        },
        "tasks": {
          "description": "Structured tasks [{id, depends_on:[ids], file}] \u2014 when present, returns a wave plan instead of executing",
          "items": {
            "properties": {
              "depends_on": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "file": {
                "type": "string"
              },
              "id": {
                "type": "string"
              }
            },
            "type": "object"
          },
          "type": "array"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "orchestrate_tasks"
  },
  {
    "description": "Run autonomous developer loop (Plan -> Execute -> Test -> Fix)",
    "inputSchema": {
      "properties": {
        "max_iterations": {
          "default": 5,
          "type": "integer"
        },
        "task": {
          "type": "string"
        }
      },
      "required": [
        "task"
      ],
      "type": "object"
    },
    "name": "run_autonomous"
  },
  {
    "description": "Check whether the assistant has device-scoped, ask-once consent for a routing question (e.g. key='opus' for 'use Opus for this?'). Device-scoped (~/.promptwise), not project-scoped -- persists across all projects on this machine. Purely advisory bookkeeping for the assistant's own behavior; has zero effect on Router.route()'s actual tier selection.",
    "inputSchema": {
      "properties": {
        "key": {
          "default": "opus",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "check_routing_consent"
  },
  {
    "description": "Record device-scoped, ask-once consent for a routing question (e.g. key='opus') so the assistant never asks again on this device. Call this after the user explicitly says yes. Purely advisory bookkeeping; has zero effect on Router.route()'s actual tier selection.",
    "inputSchema": {
      "properties": {
        "key": {
          "default": "opus",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "grant_routing_consent"
  },
  {
    "description": "Check spend against budget limit",
    "inputSchema": {
      "properties": {
        "days_elapsed": {
          "default": 1,
          "type": "integer"
        },
        "project_id": {
          "type": "string"
        },
        "tool_cost_usd": {
          "description": "Tool/API execution cost for this workflow, attributed alongside used_usd's LLM token cost",
          "type": "number"
        },
        "used_usd": {
          "type": "number"
        }
      },
      "required": [
        "used_usd"
      ],
      "type": "object"
    },
    "name": "monitor_budget"
  },
  {
    "description": "Estimate cost of a prompt before sending",
    "inputSchema": {
      "properties": {
        "model": {
          "default": "claude-sonnet-4-6",
          "type": "string"
        },
        "prompt": {
          "type": "string"
        }
      },
      "required": [
        "prompt"
      ],
      "type": "object"
    },
    "name": "predict_cost"
  },
  {
    "description": "Set monthly or daily spending limit; optionally switch the guardian between advisory (default) and opt-in hard-blocking enforcement",
    "inputSchema": {
      "properties": {
        "limit_usd": {
          "type": "number"
        },
        "mode": {
          "description": "advise (default) never blocks; block hard-stops monitor_budget once spend crosses the limit. Opt-in only.",
          "enum": [
            "advise",
            "block"
          ],
          "type": "string"
        },
        "period": {
          "default": "monthly",
          "enum": [
            "daily",
            "monthly"
          ],
          "type": "string"
        }
      },
      "required": [
        "limit_usd"
      ],
      "type": "object"
    },
    "name": "set_budget_limit"
  },
  {
    "description": "Check current spend vs configured budget limits",
    "inputSchema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_budget_status"
  },
  {
    "description": "Get detailed budget report with cost anomaly detection",
    "inputSchema": {
      "properties": {
        "period": {
          "default": "weekly",
          "enum": [
            "daily",
            "weekly",
            "monthly"
          ],
          "type": "string"
        },
        "project_id": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "budget_report"
  },
  {
    "description": "Validate generated code for syntax errors and hallucinated imports. Set use_static_analysis=true to also run a real linter (ruff for python, eslint for javascript/typescript) if installed on PATH -- opt-in, fail-open (silently skipped if the tool isn't available, never blocks the heuristic checks).",
    "inputSchema": {
      "properties": {
        "code": {
          "type": "string"
        },
        "effort_id": {
          "description": "Optional: effort_id from a prior route_request; folds this verdict back onto that effort decision's learning outcome",
          "type": "string"
        },
        "language": {
          "default": "python",
          "type": "string"
        },
        "route_id": {
          "description": "Optional: route_id from a prior route_request; folds this verdict back onto that route's learning outcome",
          "type": "string"
        },
        "technique_id": {
          "description": "Optional: technique_id from a prior suggest_technique; folds this verdict back onto that technique decision's learning outcome",
          "type": "string"
        },
        "use_static_analysis": {
          "default": false,
          "type": "boolean"
        }
      },
      "required": [
        "code"
      ],
      "type": "object"
    },
    "name": "validate_output"
  },
  {
    "description": "Per-session cost rollup: calls, cost, tokens, and tool breakdown grouped by session_id. Defaults to just the current session (current_session_only=true) -- 'what has this session cost so far'; set false to see the full multi-session breakdown. A process gets a real, distinct session_id at startup, so this is meaningful per Claude Code session, not one global bucket.",
    "inputSchema": {
      "properties": {
        "current_session_only": {
          "default": true,
          "type": "boolean"
        },
        "since": {
          "description": "ISO-8601 cutoff; omit for all history",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "session_cost_report"
  },
  {
    "description": "Calculate ROI ratio: value of time saved vs cost incurred",
    "inputSchema": {
      "properties": {
        "calls": {
          "type": "integer"
        },
        "session_id": {
          "type": "string"
        },
        "tokens_saved": {
          "type": "integer"
        },
        "total_cost_usd": {
          "type": "number"
        }
      },
      "required": [
        "session_id",
        "total_cost_usd",
        "tokens_saved",
        "calls"
      ],
      "type": "object"
    },
    "name": "track_roi"
  },
  {
    "description": "Generate team ROI report based on cumulative stats",
    "inputSchema": {
      "properties": {
        "period": {
          "default": "weekly",
          "enum": [
            "daily",
            "weekly",
            "monthly"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "get_roi_report"
  },
  {
    "description": "Get cost breakdown by project/period",
    "inputSchema": {
      "properties": {
        "format": {
          "default": "json",
          "description": "otlp emits an OpenTelemetry GenAI semantic-convention OTLP-JSON resourceMetrics payload instead of the plain report",
          "enum": [
            "json",
            "otlp"
          ],
          "type": "string"
        },
        "period": {
          "default": "weekly",
          "type": "string"
        },
        "project_id": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "cost_report"
  },
  {
    "description": "Retrieve past memory entries for a session",
    "inputSchema": {
      "properties": {
        "limit": {
          "default": 20,
          "type": "integer"
        },
        "session_id": {
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "name": "get_memory_context"
  },
  {
    "description": "Query cross-session episodic and semantic memory",
    "inputSchema": {
      "properties": {
        "query": {
          "type": "string"
        },
        "scope": {
          "default": "org",
          "enum": [
            "session",
            "org"
          ],
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "name": "query_memory"
  },
  {
    "description": "Record session activity to reset idle clock",
    "inputSchema": {
      "properties": {
        "session_id": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "ping_session"
  },
  {
    "description": "Check if session has exceeded idle thresholds",
    "inputSchema": {
      "properties": {
        "idle_threshold_minutes": {
          "default": 30,
          "type": "integer"
        },
        "session_id": {
          "type": "string"
        },
        "warn_threshold_minutes": {
          "default": 20,
          "type": "integer"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "name": "check_session_timeout"
  },
  {
    "description": "Invoke a specific skill with context",
    "inputSchema": {
      "properties": {
        "context": {
          "default": {},
          "type": "object"
        },
        "params": {
          "default": {},
          "type": "object"
        },
        "skill_name": {
          "type": "string"
        }
      },
      "required": [
        "skill_name"
      ],
      "type": "object"
    },
    "name": "invoke_skill"
  },
  {
    "description": "List all available skills filtered by role",
    "inputSchema": {
      "properties": {
        "category": {
          "type": "string"
        },
        "role": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_skills"
  },
  {
    "description": "Execute a list of skills sequentially",
    "inputSchema": {
      "properties": {
        "context": {
          "default": {},
          "type": "object"
        },
        "mode": {
          "default": "sequential",
          "enum": [
            "sequential",
            "parallel"
          ],
          "type": "string"
        },
        "skills": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "skills"
      ],
      "type": "object"
    },
    "name": "skill_chain"
  },
  {
    "description": "Recommend best skill for a given user message",
    "inputSchema": {
      "properties": {
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "suggest_skill"
  },
  {
    "description": "Auto-detect best prompting technique: CRAFT, Few-Shot, Chain-of-Thought, or Chaining. Blends the static heuristic pick with learned outcome history for this task class (fails open to the heuristic pick when history is thin). Returns technique_id -- pass it to validate_output/run_quality_gate's technique_id param so a later verdict teaches this decision.",
    "inputSchema": {
      "properties": {
        "prompt": {
          "type": "string"
        }
      },
      "required": [
        "prompt"
      ],
      "type": "object"
    },
    "name": "suggest_technique"
  },
  {
    "description": "Analyze prompt against CRAFT axes (Context/Role/Action/Format/Tone) and rebuild",
    "inputSchema": {
      "properties": {
        "prompt": {
          "type": "string"
        }
      },
      "required": [
        "prompt"
      ],
      "type": "object"
    },
    "name": "apply_craft"
  },
  {
    "description": "Enhance prompt with few-shot examples",
    "inputSchema": {
      "properties": {
        "examples": {
          "default": [],
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "prompt": {
          "type": "string"
        }
      },
      "required": [
        "prompt"
      ],
      "type": "object"
    },
    "name": "inject_few_shot"
  },
  {
    "description": "Wrap prompt with Chain-of-Thought scaffold",
    "inputSchema": {
      "properties": {
        "prompt": {
          "type": "string"
        },
        "style": {
          "default": "step-by-step",
          "enum": [
            "standard",
            "step-by-step",
            "tree-of-thought"
          ],
          "type": "string"
        }
      },
      "required": [
        "prompt"
      ],
      "type": "object"
    },
    "name": "add_chain_of_thought"
  },
  {
    "description": "Decompose complex task into sequential prompt chain",
    "inputSchema": {
      "properties": {
        "steps": {
          "default": 3,
          "type": "integer"
        },
        "task": {
          "type": "string"
        }
      },
      "required": [
        "task"
      ],
      "type": "object"
    },
    "name": "chain_prompts"
  },
  {
    "description": "Estimate cost and recommend model tier across Haiku/Sonnet/Opus",
    "inputSchema": {
      "properties": {
        "prompt": {
          "type": "string"
        },
        "task_type": {
          "default": "general",
          "type": "string"
        }
      },
      "required": [
        "prompt"
      ],
      "type": "object"
    },
    "name": "eval_prompt_across_models"
  },
  {
    "description": "Score system prompt on clarity, role, constraints, and jailbreak resistance",
    "inputSchema": {
      "properties": {
        "system_prompt": {
          "type": "string"
        }
      },
      "required": [
        "system_prompt"
      ],
      "type": "object"
    },
    "name": "audit_system_prompt"
  },
  {
    "description": "Save a prompt to the versioned prompt registry",
    "inputSchema": {
      "properties": {
        "content": {
          "type": "string"
        },
        "description": {
          "default": "",
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "tags": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "version": {
          "default": "1.0.0",
          "type": "string"
        }
      },
      "required": [
        "name",
        "content"
      ],
      "type": "object"
    },
    "name": "save_prompt"
  },
  {
    "description": "Search prompts in the versioned prompt registry",
    "inputSchema": {
      "properties": {
        "query": {
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "name": "search_prompts"
  },
  {
    "description": "Diff two versions of a registered prompt",
    "inputSchema": {
      "properties": {
        "name": {
          "type": "string"
        },
        "version_a": {
          "type": "string"
        },
        "version_b": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "version_a",
        "version_b"
      ],
      "type": "object"
    },
    "name": "compare_prompts"
  },
  {
    "description": "Roll a named prompt back to an earlier version's content by writing it as a new, current registry entry -- existing history rows are never mutated or deleted",
    "inputSchema": {
      "properties": {
        "name": {
          "type": "string"
        },
        "version": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "version"
      ],
      "type": "object"
    },
    "name": "rollback_prompt"
  },
  {
    "description": "Replay a registered prompt version's content through the eval harness's rubric cases to score its quality (offline record/dry-run by default), optionally diffing against another version for regression comparison",
    "inputSchema": {
      "properties": {
        "bar": {
          "default": 0.6,
          "type": "number"
        },
        "cases": {
          "default": [],
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "cases_path": {
          "default": "",
          "type": "string"
        },
        "compare_version": {
          "default": "",
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "tiers": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "version": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "version"
      ],
      "type": "object"
    },
    "name": "replay_prompt_version"
  },
  {
    "description": "Get session usage statistics",
    "inputSchema": {
      "properties": {
        "since": {
          "description": "ISO 8601 timestamp filter",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "get_session_stats"
  },
  {
    "description": "Delete usage history older than N days",
    "inputSchema": {
      "properties": {
        "older_than_days": {
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "older_than_days"
      ],
      "type": "object"
    },
    "name": "clear_history"
  },
  {
    "description": "Export usage history as JSON",
    "inputSchema": {
      "properties": {
        "format": {
          "default": "json",
          "enum": [
            "json",
            "csv"
          ],
          "type": "string"
        },
        "since": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "export_stats"
  },
  {
    "description": "Reload configuration without restarting server",
    "inputSchema": {
      "properties": {},
      "type": "object"
    },
    "name": "reload_config"
  },
  {
    "description": "Get energy efficiency score for a model",
    "inputSchema": {
      "properties": {
        "model": {
          "type": "string"
        },
        "tokens": {
          "default": 1000,
          "type": "integer"
        }
      },
      "required": [
        "model"
      ],
      "type": "object"
    },
    "name": "check_energy"
  },
  {
    "description": "Detect applicable plugin for text",
    "inputSchema": {
      "properties": {
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "route_for_plugin"
  },
  {
    "description": "Estimate and compare per-model cost for a prompt across multiple models (cost only -- for a real quality comparison, use run_eval_harness)",
    "inputSchema": {
      "properties": {
        "models": {
          "default": [
            "claude-haiku-4-5-20251001",
            "claude-sonnet-4-6",
            "claude-opus-4-7"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "prompt": {
          "type": "string"
        }
      },
      "required": [
        "prompt"
      ],
      "type": "object"
    },
    "name": "run_eval"
  },
  {
    "description": "Run a durable eval + regression suite (prompt+rubric cases) offline; score with the quality gate, diff against a stored baseline to flag regressions, expose a pass/fail gate, and feed outcomes back into adaptive routing. Offline default is a record/dry-run mode (no cloud). Set save_baseline=true to bless this run as the new baseline.",
    "inputSchema": {
      "properties": {
        "bar": {
          "default": 0.6,
          "type": "number"
        },
        "cases": {
          "default": [],
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "cases_path": {
          "default": "",
          "type": "string"
        },
        "save_baseline": {
          "default": false,
          "type": "boolean"
        },
        "suite": {
          "default": "default",
          "type": "string"
        },
        "tiers": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "name": "run_eval_harness"
  },
  {
    "description": "Generate SBOM in CycloneDX format",
    "inputSchema": {
      "properties": {
        "format": {
          "default": "cyclonedx",
          "enum": [
            "cyclonedx",
            "spdx"
          ],
          "type": "string"
        },
        "paths": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "name": "get_sbom"
  },
  {
    "description": "Run all security checks as a suite",
    "inputSchema": {
      "properties": {
        "context": {
          "type": "object"
        },
        "targets": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "name": "run_security_suite"
  },
  {
    "description": "Run a durable, offline red-team suite against the security scanners: known attack patterns (must be caught) and benign counterexamples (must NOT be flagged) across injection/owasp/secrets/destructive/permissions/pii/supply_chain checks. Diffs against a stored baseline to flag regressions (an attack that used to be caught now escapes, or a benign input starts false-positiving) and exposes a pass/fail gate. Defaults to a built-in corpus when no cases/cases_path given. Set save_baseline=true to bless this run as the new baseline.",
    "inputSchema": {
      "properties": {
        "cases": {
          "default": [],
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "cases_path": {
          "default": "",
          "type": "string"
        },
        "save_baseline": {
          "default": false,
          "type": "boolean"
        },
        "suite": {
          "default": "default",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "run_red_team_harness"
  },
  {
    "description": "Two-phase, persona-aware agile plan (analyst->pm->[ux]->architect->po, then per-story sm->dev->qa loop) layered on the workflow classifier; carries the compliance gate and model-tier routing",
    "inputSchema": {
      "properties": {
        "brownfield": {
          "type": "boolean"
        },
        "regulated": {
          "type": "boolean"
        },
        "task": {
          "type": "string"
        }
      },
      "required": [
        "task"
      ],
      "type": "object"
    },
    "name": "agile_plan"
  },
  {
    "description": "Split a PRD/architecture markdown document into focused, anchored shards by heading level",
    "inputSchema": {
      "properties": {
        "by_level": {
          "default": 2,
          "type": "integer"
        },
        "markdown": {
          "type": "string"
        }
      },
      "required": [
        "markdown"
      ],
      "type": "object"
    },
    "name": "shard_doc"
  },
  {
    "description": "Assemble a self-contained, context-engineered story: embeds architecture shards, constraints, and compliance rules inline so the dev executor needs no external lookup",
    "inputSchema": {
      "properties": {
        "acceptance_criteria": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "arch_shards": {
          "default": [],
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "compliance_rules": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "constraints": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "epic_id": {
          "default": "",
          "type": "string"
        },
        "files_to_touch": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "story_id": {
          "type": "string"
        },
        "tasks": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "story_id",
        "title"
      ],
      "type": "object"
    },
    "name": "draft_story"
  },
  {
    "description": "Issue an advisory, auditable quality-gate decision (PASS/CONCERNS/FAIL/WAIVED) from findings, risk score, and NFR assessment",
    "inputSchema": {
      "properties": {
        "effort_id": {
          "description": "Optional: effort_id from a prior route_request; folds this gate verdict back onto that effort decision's learning outcome",
          "type": "string"
        },
        "findings": {
          "default": [],
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "nfr_assessment": {
          "default": {},
          "type": "object"
        },
        "risk_score": {
          "default": 0,
          "type": "integer"
        },
        "route_id": {
          "description": "Optional: route_id from a prior route_request; folds this gate verdict back onto that route's learning outcome",
          "type": "string"
        },
        "story_id": {
          "type": "string"
        },
        "technique_id": {
          "description": "Optional: technique_id from a prior suggest_technique; folds this gate verdict back onto that technique decision's learning outcome",
          "type": "string"
        },
        "waiver_reason": {
          "default": "",
          "type": "string"
        }
      },
      "required": [
        "story_id"
      ],
      "type": "object"
    },
    "name": "run_quality_gate"
  },
  {
    "description": "Evaluate a proposed action (model tier, cost, operation, gates) against the cross-agent governance policy; returns allow/block with recorded reasons. The policy file may optionally set extends: <parent-path> for org->team->project inheritance -- a child can only tighten (narrow allowed tiers, add bans/gates, lower the budget cap), never loosen a parent's policy. Single-tier files with no extends key behave exactly as before.",
    "inputSchema": {
      "properties": {
        "estimated_cost": {
          "type": "number"
        },
        "gates_passed": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "model_tier": {
          "type": "string"
        },
        "operation": {
          "type": "string"
        },
        "policy_path": {
          "default": "config/policy.yaml",
          "type": "string"
        },
        "spent_so_far": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "name": "check_policy"
  },
  {
    "description": "Append a tamper-evident, hash-chained audit record of an AI-assisted change ('the trace'); returns the record and chain verification status",
    "inputSchema": {
      "properties": {
        "agent": {
          "default": "",
          "type": "string"
        },
        "compliance_decision": {
          "default": "",
          "type": "string"
        },
        "cost_usd": {
          "default": 0.0,
          "type": "number"
        },
        "files_touched": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "gate_decision": {
          "default": "",
          "type": "string"
        },
        "model": {
          "default": "",
          "type": "string"
        },
        "rules_applied": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "task": {
          "type": "string"
        }
      },
      "required": [
        "task"
      ],
      "type": "object"
    },
    "name": "record_audit"
  },
  {
    "description": "Export the full AI-change audit trail (portable JSON + human-readable text) with hash-chain verification status",
    "inputSchema": {
      "properties": {
        "format": {
          "default": "both",
          "description": "otlp emits an OpenTelemetry GenAI semantic-convention OTLP-JSON resourceSpans payload (one span per audit record) instead of json/text",
          "enum": [
            "json",
            "text",
            "both",
            "otlp"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "export_audit"
  },
  {
    "description": "Compile one governance source (policy + packs + method) into every agent's native rules file (CLAUDE.md, AGENTS.md, .cursor/rules, copilot-instructions, .clinerules, GEMINI.md, .windsurfrules, .aiassistant/rules). Non-destructive: only the managed block is regenerated; user edits are preserved",
    "inputSchema": {
      "properties": {
        "adopt": {
          "default": false,
          "type": "boolean"
        },
        "mode": {
          "default": "apply",
          "enum": [
            "apply",
            "preview",
            "check"
          ],
          "type": "string"
        },
        "packs": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "path_rules": {
          "additionalProperties": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "description": "glob -> path-scoped rules (Copilot .github/instructions/*)",
          "type": "object"
        },
        "policy_summary": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "project": {
          "type": "string"
        },
        "repo_root": {
          "default": ".",
          "type": "string"
        },
        "rules": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "targets": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    },
    "name": "sync_agent_config"
  },
  {
    "description": "Detect which coding agents a repo is configured for (CLAUDE.md, AGENTS.md, .cursor/rules, copilot) + confidence + recommended targets",
    "inputSchema": {
      "properties": {
        "repo_root": {
          "default": ".",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "detect_agents"
  },
  {
    "description": "Derive structured intent/role/stack/domain/regulated context from a prompt (+ optional repo) to drive config emission",
    "inputSchema": {
      "properties": {
        "repo_root": {
          "default": ".",
          "type": "string"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "build_context_model"
  },
  {
    "description": "Preview a unified diff of the agent rules files PromptWise would write, per target, WITHOUT writing \u2014 the review step before apply",
    "inputSchema": {
      "properties": {
        "adopt": {
          "default": false,
          "type": "boolean"
        },
        "packs": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "path_rules": {
          "additionalProperties": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "type": "object"
        },
        "policy_summary": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "project": {
          "type": "string"
        },
        "repo_root": {
          "default": ".",
          "type": "string"
        },
        "rules": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "targets": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    },
    "name": "propose_agent_config"
  },
  {
    "description": "Lint an agent rules file (or content) for token tax, byte caps, missing .mdc frontmatter, and inferable bloat",
    "inputSchema": {
      "properties": {
        "always_apply": {
          "default": false,
          "type": "boolean"
        },
        "content": {
          "type": "string"
        },
        "fmt": {
          "default": "md",
          "enum": [
            "md",
            "mdc"
          ],
          "type": "string"
        },
        "max_bytes": {
          "type": "integer"
        },
        "path": {
          "type": "string"
        },
        "token_budget": {
          "default": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "name": "lint_agent_config"
  },
  {
    "description": "Cross-host portability check (Phase 7 \u00a77.4): verify the emitted governance configs for every supported host (CLAUDE.md, AGENTS.md, .cursor/rules, copilot, .clinerules, GEMINI.md, .windsurfrules, .aiassistant/rules) are present, well-formed, and in sync with the current skill/agent surface (skill_packs / agents / commands); reports drift precisely. Set emit_ci to also return a host-neutral CI-snippet that runs the governance gates using tiers/families only. Offline.",
    "inputSchema": {
      "properties": {
        "emit_ci": {
          "default": false,
          "description": "also return a host-neutral CI-snippet",
          "type": "boolean"
        },
        "hosts": {
          "description": "subset of supported hosts to check; default all",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "repo_root": {
          "default": ".",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "check_portability"
  },
  {
    "description": "Flatten one governance source (policy + packs + method) into a SINGLE self-contained, pasteable file for web-chat hosts with no IDE/CLI/MCP support (ChatGPT, Gemini, Claude.ai web chat). Not a managed-block IDE config: every call fully regenerates the bundle, there is no user-owned region to preserve. Set out_path to also write it to disk.",
    "inputSchema": {
      "properties": {
        "include_packs": {
          "default": true,
          "type": "boolean"
        },
        "out_path": {
          "description": "optional path to also write the bundle as a single file",
          "type": "string"
        },
        "packs": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "policy_summary": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "project": {
          "type": "string"
        },
        "rules": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "skill_root": {
          "default": "skill_packs",
          "type": "string"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    },
    "name": "export_web_bundle"
  },
  {
    "description": "Store a correction as a durable, searchable learning (category, mistake, fix, project). Local SQLite + FTS5, offline.",
    "inputSchema": {
      "properties": {
        "category": {
          "description": "e.g. 'style', 'security', 'api-misuse'",
          "type": "string"
        },
        "correction": {
          "description": "the fix / the rule going forward",
          "type": "string"
        },
        "mistake": {
          "description": "what went wrong",
          "type": "string"
        },
        "project": {
          "default": "",
          "type": "string"
        },
        "tags": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "category",
        "mistake",
        "correction"
      ],
      "type": "object"
    },
    "name": "capture_learning"
  },
  {
    "description": "Top-K relevant past corrections for a task description (FTS5 BM25, LIKE fallback) plus a ready-to-inject reminder block.",
    "inputSchema": {
      "properties": {
        "k": {
          "default": 5,
          "maximum": 25,
          "minimum": 1,
          "type": "integer"
        },
        "project": {
          "type": "string"
        },
        "task": {
          "type": "string"
        }
      },
      "required": [
        "task"
      ],
      "type": "object"
    },
    "name": "replay_learnings"
  },
  {
    "description": "Correction trends from the local learning store: counts by category, project, month, and the most-repeated mistakes.",
    "inputSchema": {
      "properties": {},
      "type": "object"
    },
    "name": "learning_insights"
  },
  {
    "description": "Ranked, actionable recommendations over local telemetry: routing downgrades/escalations, top cost drivers & spend anomalies, quality/eval regressions, and budget projections. Deterministic, offline, min-sample gated.",
    "inputSchema": {
      "properties": {
        "top_n": {
          "default": 10,
          "maximum": 100,
          "minimum": 1,
          "type": "integer"
        },
        "window_days": {
          "default": 30,
          "description": "analysis window for cost/quality/budget families",
          "maximum": 365,
          "minimum": 1,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "name": "insights_report"
  },
  {
    "description": "Learn allow/deny permission suggestions from denial telemetry (the Phase 1 PermissionDenied log). Proposals only \u2014 never edits config.",
    "inputSchema": {
      "properties": {
        "mcp_json": {
          "description": "path to .mcp.json for the current allowlist",
          "type": "string"
        },
        "min_count": {
          "default": 2,
          "minimum": 1,
          "type": "integer"
        },
        "state_dir": {
          "default": ".",
          "description": "project dir holding .promptwise/denials.jsonl",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "tune_permissions"
  },
  {
    "description": "Audit declared MCP servers (.mcp.json + plugin.json) for security flags, allow-surface, and redundancy. Offline; inspects config, does not call servers.",
    "inputSchema": {
      "properties": {
        "extra_configs": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "repo_root": {
          "default": ".",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "audit_mcp_servers"
  },
  {
    "description": "Search the trace (hash-chained audit trail + learnings) by meaning. Keyword/FTS by default; optional local embeddings if installed and enabled. Offline.",
    "inputSchema": {
      "properties": {
        "audit_path": {
          "type": "string"
        },
        "k": {
          "default": 5,
          "maximum": 25,
          "minimum": 1,
          "type": "integer"
        },
        "query": {
          "type": "string"
        },
        "repo_root": {
          "default": ".",
          "type": "string"
        },
        "use_embeddings": {
          "default": false,
          "type": "boolean"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "name": "search_trace"
  },
  {
    "description": "Retrieval-augmented context manager: rank and prune candidates from the trace (audit + learnings) and an optionally-supplied doc onto one token budget. No new ranking algorithm - reuses search_trace's keyword/BM25 (or optional embeddings) scoring; docs are sharded per call, not indexed. Offline.",
    "inputSchema": {
      "properties": {
        "audit_path": {
          "type": "string"
        },
        "doc_path": {
          "type": "string"
        },
        "doc_text": {
          "type": "string"
        },
        "learning_db": {
          "type": "string"
        },
        "query": {
          "type": "string"
        },
        "repo_root": {
          "default": ".",
          "type": "string"
        },
        "sources": {
          "default": [
            "audit",
            "learnings",
            "doc"
          ],
          "items": {
            "enum": [
              "audit",
              "learnings",
              "doc"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "token_budget": {
          "default": 2000,
          "type": "integer"
        },
        "use_embeddings": {
          "default": false,
          "type": "boolean"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "name": "rank_context"
  },
  {
    "description": "Grant a time-boxed permission for a tool signature (e.g. 'Bash:git'), auto-expiring after ttl_minutes (default 60, max 480/8h). Independent of .mcp.json; enforced by the PreToolUse JIT guard hook, which auto-approves while active and falls back to the normal permission prompt once expired. Note: matching is coarse (first command token only, e.g. 'Bash:git' matches any git invocation) -- the same grain permission_tuner's suggestions use; grant narrowly-scoped, short-lived permissions for commands you trust broadly, not ones where the arguments matter.",
    "inputSchema": {
      "properties": {
        "signature": {
          "description": "tool signature, e.g. 'Bash:git' or 'mcp__promptwise__run_governor'",
          "type": "string"
        },
        "ttl_minutes": {
          "default": 60,
          "maximum": 480,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "signature"
      ],
      "type": "object"
    },
    "name": "grant_jit_permission"
  },
  {
    "description": "Immediately revoke a time-boxed JIT permission grant for a tool signature, before its natural expiry.",
    "inputSchema": {
      "properties": {
        "signature": {
          "type": "string"
        }
      },
      "required": [
        "signature"
      ],
      "type": "object"
    },
    "name": "revoke_jit_permission"
  },
  {
    "description": "List all JIT permission grants (active and expired) with their expiry timestamps.",
    "inputSchema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_jit_permissions"
  },
  {
    "description": "Fold accumulated corrections (Phase 2 learning store) into a SKILL.md as a stamped, reversible managed block. Accepts the patch only if the pack's quality score strictly improves. Offline; no model required.",
    "inputSchema": {
      "properties": {
        "dry_run": {
          "default": false,
          "description": "score and preview without writing",
          "type": "boolean"
        },
        "max_rules": {
          "default": 8,
          "maximum": 25,
          "minimum": 1,
          "type": "integer"
        },
        "project": {
          "description": "scope corrections to a project",
          "type": "string"
        },
        "skill_path": {
          "description": "path to the SKILL.md / pack .md to optimize",
          "type": "string"
        }
      },
      "required": [
        "skill_path"
      ],
      "type": "object"
    },
    "name": "optimize_skill_pack"
  },
  {
    "description": "Build a self-verifying, signed compliance evidence bundle from the hash-chained audit trail: verifies the chain, wraps records in a manifest (time range, count, chain-head digest), signs with a local key, and can write a .zip. Offline; no network.",
    "inputSchema": {
      "properties": {
        "control_families": {
          "description": "generic control-family tags; inferred from the trace when omitted",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "out_path": {
          "description": "optional path to write a .zip evidence archive",
          "type": "string"
        },
        "sign": {
          "default": true,
          "description": "sign the bundle with the local key (env PROMPTWISE_AUDIT_KEY/PROMPTWISE_AUDIT_KEY_FILE for hmac, PROMPTWISE_ED25519_KEY/PROMPTWISE_ED25519_KEY_FILE for ed25519)",
          "type": "boolean"
        },
        "sign_alg": {
          "default": "hmac",
          "description": "hmac (shared-secret, default) or ed25519 (asymmetric, verifiable by a third party without the private key)",
          "enum": [
            "hmac",
            "ed25519"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "export_compliance_bundle"
  },
  {
    "description": "Generate a fresh Ed25519 keypair for compliance bundle signing. Returned in-memory only -- never written to disk. Store the private_key yourself (e.g. as PROMPTWISE_ED25519_KEY) before it is lost; share the public_key with auditors who need to verify bundles you sign. Note: a bundle verifying with signature_ok=True only proves internal self-consistency (signed by *some* keypair), not authenticity -- auditors must compare the bundle's embedded public_key against the public_key you shared with them out-of-band.",
    "inputSchema": {
      "properties": {},
      "type": "object"
    },
    "name": "generate_ed25519_keypair"
  },
  {
    "description": "Build a periodic summary (spend, security-scan verdicts, governance/governor actions) for a stakeholder who doesn't touch a CLI. Markdown or self-contained HTML; no PDF dependency. Pass out_path to write it; omit to just return the report data. Offline; no network.",
    "inputSchema": {
      "properties": {
        "format": {
          "default": "markdown",
          "enum": [
            "markdown",
            "html"
          ],
          "type": "string"
        },
        "out_path": {
          "description": "optional path to write the report",
          "type": "string"
        },
        "repo_root": {
          "default": ".",
          "type": "string"
        },
        "window_days": {
          "default": 30,
          "maximum": 365,
          "minimum": 1,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "name": "export_org_report"
  },
  {
    "description": "Turn local insights recommendations into typed, policy-gated, REVERSIBLE governance actions. Default advise-only: proposes actions + policy verdicts and reports what would/did apply for the current mode (env PROMPTWISE_AUTONOMY in {advise,dry_run,apply}). Only allowlisted 'safe' actions (AdjustBudgetGuard, WriteRoutingPreferenceNote) can ever move state, and only in apply; each writes a local undo-ledger entry. Everything else is advisory-only. Offline; fully audited.",
    "inputSchema": {
      "properties": {
        "mode": {
          "description": "override PROMPTWISE_AUTONOMY for this call; omit to use the env (default advise)",
          "enum": [
            "advise",
            "dry_run",
            "apply"
          ],
          "type": "string"
        },
        "policy_path": {
          "default": "config/policy.yaml",
          "type": "string"
        },
        "root": {
          "default": ".",
          "description": "project root holding the gitignored .promptwise overlay + ledger",
          "type": "string"
        },
        "window_days": {
          "default": 30,
          "maximum": 365,
          "minimum": 1,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "name": "run_governor"
  },
  {
    "description": "Reverse a previously-applied governor action by id, restoring the exact prior state from the undo ledger (audited). No-op if the id is unknown or non-reversible.",
    "inputSchema": {
      "properties": {
        "action_id": {
          "type": "string"
        },
        "root": {
          "default": ".",
          "type": "string"
        }
      },
      "required": [
        "action_id"
      ],
      "type": "object"
    },
    "name": "governor_undo"
  },
  {
    "description": "Record an architectural decision (ADR-style): title, context (the situation/forces), decision (what was chosen), and optional consequences/tags. Pass supersedes=<id> to atomically mark an earlier decision as superseded and link it -- one call, no separate supersede step.",
    "inputSchema": {
      "properties": {
        "consequences": {
          "default": "",
          "type": "string"
        },
        "context": {
          "type": "string"
        },
        "decision": {
          "type": "string"
        },
        "status": {
          "default": "accepted",
          "enum": [
            "proposed",
            "accepted",
            "deprecated"
          ],
          "type": "string"
        },
        "supersedes": {
          "description": "id of an earlier decision this one replaces",
          "type": "integer"
        },
        "tags": {
          "default": "",
          "description": "comma-joined free-form tags, e.g. 'routing,cost'",
          "type": "string"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "title",
        "context",
        "decision"
      ],
      "type": "object"
    },
    "name": "record_decision"
  },
  {
    "description": "Query the ADR/decision-memory log. Pass query for a case-insensitive keyword search across title/context/decision/consequences; otherwise lists decisions optionally filtered by status (proposed/accepted/superseded/deprecated) and/or tag (exact tag match, not substring). Newest first.",
    "inputSchema": {
      "properties": {
        "limit": {
          "default": 20,
          "type": "integer"
        },
        "query": {
          "default": "",
          "type": "string"
        },
        "status": {
          "default": "",
          "type": "string"
        },
        "tag": {
          "default": "",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "query_decisions"
  }
]